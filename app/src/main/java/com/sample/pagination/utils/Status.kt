package com.sample.pagination.utils

// Define an enum class to represent different states of an API operation
enum class Status {
    // Represents a successful operation
    SUCCESS,
    // Represents an error or failure in the operation
    ERROR,
    // Represents that the operation is currently in progress or loading
    LOADING
}